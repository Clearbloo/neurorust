[package]
name = "matrix"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.11.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
nalgebra = "0.33"  # Popular matrix library to compare against

[[bench]]
name = "matrix_bench"
harness = false 
